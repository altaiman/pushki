mixin field(data)
  +b.LABEL.field(class=data.style ? '_'+data.style : '')
    +e.DIV.input
      - if (data.name)
        +b.SPAN.subcaption= data.name
      input(type=data.type value=data.value placeholder=data.placeholder)&attributes({'data-masked': data.masked})
